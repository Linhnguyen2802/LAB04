@model IEnumerable<string>
@{
    ViewBag.Title = "getCompany";
}

<div id="4">
    @foreach(var item in Model)
    {
        @Html.CheckBox(item);
        @Html.Label(item);
        <b>&nbsp</b>
    }
</div>

